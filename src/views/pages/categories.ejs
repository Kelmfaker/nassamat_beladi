<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8">
  <title>إدارة الفئات - Thinker Honey</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <style>
    .dive {
      background: #fff;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
      margin-bottom: 30px;
      max-width: 600px;
      margin-left: auto;
      margin-right: auto;
    }

    .dive h3 {
      text-align: center;
      color: #825221;
      margin-bottom: 20px;
    }

    .dive form {
      display: flex;
      gap: 10px;
      flex-direction: column;
    }

    .table-container {
      background: #fff;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
      margin: 20px auto;
      max-width: 1000px;
    }

    .table-container h3 {
      text-align: center;
      color: #825221;
      margin-bottom: 20px;
    }

    .flex {
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .flex input {
      flex: 1;
      margin: 0;
    }

    .flex button {
      width: auto;
      margin: 0;
      padding: 8px 20px;
    }

    td form {
      margin: 0;
    }

    td button {
      width: auto;
      padding: 8px 20px;
      margin: 5px;
    }

    .delete-btn {
      background-color: #dc3545 !important;
    }

    .delete-btn:hover {
      background-color: #c82333 !important;
    }

    .edit-btn {
      background-color: #0D8442 !important;
    }

    .edit-btn:hover {
      background-color: #0a6b35 !important;
    }
  </style>
</head>
<body>

  <%- include('../partials/header') %>

  <div class="content">
    <h1 style="text-align: center; color: #825221; margin-bottom: 30px;">إدارة الفئات</h1>

    <!-- إضافة فئة -->
    <div class="dive">
      <h3>إضافة فئة جديدة</h3>
      <form action="/admin/categories/add" method="POST">
        <input type="text" name="name" placeholder="اسم الفئة" required>
        <button type="submit">إضافة</button>
      </form>
    </div>

    <!-- قائمة الأصناف -->
    <div class="table-container">
      <h3>قائمة الأصناف</h3>

      <% if (categories && categories.length > 0) { %>
        <table>
          <thead>
            <tr>
              <th>#</th>
              <th>الاسم</th>
              <th>إجراءات</th>
            </tr>
          </thead>
          <tbody>
            <% categories.forEach((cat, index) => { %>
              <tr>
                <td><%= index + 1 %></td>
                <td>
                  <form action="/admin/categories/edit/<%= cat._id %>" method="POST" class="flex">
                    <input type="text" name="name" value="<%= cat.name %>" required>
                    <button type="submit" class="edit-btn">تعديل</button>
                  </form>
                </td>
                <td>
                  <form action="/admin/categories/delete/<%= cat._id %>" method="GET" onsubmit="return confirm('هل أنت متأكد من الحذف؟')" style="display: inline;">
                    <button type="submit" class="delete-btn">حذف</button>
                  </form>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      <% } else { %>
        <div style="text-align: center; padding: 50px; color: #825221;">
          <h3>لا توجد أصناف</h3>
          <p>قم بإضافة صنف جديد من الأعلى</p>
        </div>
      <% } %>
    </div>
  </div>

  <%- include('../partials/footer') %>

  <script src="/js/main.js"></script>
</body>
</html>
