<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>لوحة التحكم - إدارة</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <%- include('../../partials/header') %>

  <main class="content admin-dashboard">
    <div class="dashboard-container">
      <header class="dashboard-header">
        <h1>لوحة التحكم</h1>
        <div class="dashboard-actions">
          <a href="/admin/products/create" class="btn btn-primary">إنشاء منتج</a>
          <a href="/admin/orders" class="btn btn-secondary">عرض الطلبات</a>
          <a href="/admin/products" class="btn btn-light">جميع المنتجات</a>
        </div>
      </header>

      <section class="dashboard-stats">
        <div class="dashboard-grid">
          <a href="/admin/products" class="dashboard-card-link">
            <div class="stat-card">
              <div class="stat-icon">📦</div>
              <div class="stat-body">
                <div class="stat-title">المنتجات</div>
                <div class="stat-value"><%= productsCount %></div>
              </div>
            </div>
          </a>

          <a href="/admin/users" class="dashboard-card-link">
            <div class="stat-card">
              <div class="stat-icon">👥</div>
              <div class="stat-body">
                <div class="stat-title">المستخدمون</div>
                <div class="stat-value"><%= usersCount %></div>
              </div>
            </div>
          </a>

          <a href="/admin/orders" class="dashboard-card-link">
            <div class="stat-card">
              <div class="stat-icon">🧾</div>
              <div class="stat-body">
                <div class="stat-title">الطلبات</div>
                <div class="stat-value"><%= ordersCount %></div>
              </div>
            </div>
          </a>

          <a href="/admin/orders" class="dashboard-card-link">
            <div class="stat-card">
              <div class="stat-icon">💰</div>
              <div class="stat-body">
                <div class="stat-title">الإيرادات (مدفوعة)</div>
                <div class="stat-value"><%= totalRevenue.toFixed(2) %> <%= currencySymbol %></div>
              </div>
            </div>
          </a>
        </div>
      </section>

      <section class="dashboard-charts">
        <h3>مخططات آخر 7 أيام</h3>
        <div id="chartsPanel" class="charts-panel">
          <div class="card chart-card">
            <h4 class="chart-title">الطلبات</h4>
            <div class="chart-wrap"><canvas id="ordersChart"></canvas></div>
          </div>
          <div class="card chart-card">
            <h4 class="chart-title">الإيرادات</h4>
            <div class="chart-wrap"><canvas id="revenueChart"></canvas></div>
          </div>
        </div>
      </section>

      <section class="recent-orders">
        <h2>أحدث الطلبات</h2>
        <% if (recentOrders && recentOrders.length) { %>
          <div class="table-container">
            <table class="admin-table">
              <thead>
                <tr>
                  <th>رقم الطلب</th>
                  <th>المستخدم</th>
                  <th>المجموع</th>
                  <th>الحالة</th>
                  <th>تاريخ</th>
                </tr>
              </thead>
              <tbody>
                <% recentOrders.forEach(o => { %>
                  <tr>
                    <td><%= o.orderNumber %></td>
                    <td><%= o.user ? o.user.name : 'ضيف' %></td>
                    <td><%= o.totalAmount.toFixed(2) %> <%= currencySymbol %></td>
                    <td><%= o.status %></td>
                    <td><%= new Date(o.createdAt).toLocaleString('ar-EG-u-nu-latn') %></td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
          </div>
        <% } else { %>
          <p>لا توجد طلبات حديثة.</p>
        <% } %>
      </section>

    </div>
  </main>

  <%- include('../../partials/footer.ejs') %>
  <!-- Chart data (injected) -->
  <script>
    // Minimal injection for external admin-dashboard.js to consume
    window.adminChartData = <%- JSON.stringify(chart || { labels: [], ordersData: [], revenueData: [] }) %>;
  </script>
</body>
</html>
