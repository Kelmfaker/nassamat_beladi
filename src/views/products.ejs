<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>المنتجات - نسمات بلادي</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
  <%- include("partials/header") %>

  <section class="products-content">
    <div class="products-container">
      <div class="products-header">
        <h1><i class="fas fa-seedling"></i> المنتجات</h1>
        <p>أفضل المنتجات الطبيعية من نسمات بلادي</p>
        <div class="category-meta" style="margin-top:.25rem; display:flex; gap:.5rem; align-items:center;">
          <p class="category-description" style="margin:0;color:#666;font-size:.95rem;"></p>
          <button id="clearFilterBtn" type="button" class="btn clear-filter" style="display:none;">مسح الفلتر</button>
        </div>
      </div>

      <div class="products-toolbar">
        <div class="search-box">
          <i class="fas fa-search"></i>
          <input type="search" name="q" placeholder="ابحث عن منتج..." id="searchInput">
        </div>
        <div class="toolbar-actions">
          <select class="sort-select" name="sort" id="sortSelect">
            <option value="new" <%= typeof selectedSort !== 'undefined' && selectedSort === 'new' ? 'selected' : '' %>>الأحدث</option>
            <option value="price_asc" <%= typeof selectedSort !== 'undefined' && selectedSort === 'price_asc' ? 'selected' : '' %>>السعر: من الأرخص</option>
            <option value="price_desc" <%= typeof selectedSort !== 'undefined' && selectedSort === 'price_desc' ? 'selected' : '' %>>السعر: من الأعلى</option>
            <option value="name" <%= typeof selectedSort !== 'undefined' && selectedSort === 'name' ? 'selected' : '' %>>الاسم: أ-ي</option>
            <option value="reviews" <%= typeof selectedSort !== 'undefined' && selectedSort === 'reviews' ? 'selected' : '' %>>الأعلى تقييماً</option>
          </select>
          <div class="view-toggle">
            <button type="button" class="active" data-view="grid"><i class="fas fa-border-all"></i></button>
            <button type="button" data-view="list"><i class="fas fa-list-ul"></i></button>
          </div>
        </div>
      </div>

      <div class="filter-chips">
        <!-- Category select dropdown (single-select). Replaces chips/list UI. -->
        <label for="categorySelect" style="display:flex;gap:.5rem;align-items:center;">
          <select id="categorySelect" name="category" style="padding:.35rem .6rem;border-radius:6px;border:1px solid #ddd;background:#fff;">
            <option value="all">الكل</option>
            <% if (categories && categories.length > 0) { %>
              <% categories.forEach(cat => { %>
                <option value="<%= cat._id %>" data-desc="<%= (cat.description || '') %>"><%= cat.name %></option>
              <% }) %>
            <% } %>
          </select>
        </label>
      </div>

  <div class="store">
    <div class="table-container">
      <% if (products && products.length > 0) { %>
        <table class="admin-products-table public-products-table">
          <thead>
            <tr>
              <th>صورة</th>
              <th>الاسم</th>
              <th>القسم</th>
              <th>الوصف</th>
              <th>السعر</th>
              <th>إجراءات</th>
            </tr>
          </thead>
          <tbody>
            <% products.forEach(product => { %>
              <tr data-product-id="<%= product._id %>" data-category-id="<%= product.category?._id || '' %>">
                <td class="td-img">
                  <% if (product.image) { const imgSrc = product.image.startsWith('/') ? product.image : ('/images/' + product.image); %>
                    <img src="<%= imgSrc %>" alt="<%= product.name %>" width="72" height="72" style="object-fit:cover;border-radius:6px;" />
                  <% } else { %>
                    <img src="/images/default-product.svg" alt="<%= product.name %>" width="72" height="72" style="object-fit:contain;border-radius:6px;background:#fff;padding:6px;" />
                  <% } %>
                </td>
                <td class="td-name">
                  <div style="display:flex;flex-direction:column;gap:.25rem;">
                    <span><%= product.name %></span>
                    <% if (typeof product.avgRating !== 'undefined') { %>
                      <small class="prod-rating" style="color:#f39c12;font-weight:600;"> <%= (product.avgRating ? product.avgRating.toFixed(1) : '0.0') %> ★</small>
                    <% } %>
                  </div>
                </td>
                <td class="td-cat"><%= product.category?.name || 'بدون قسم' %></td>
                <td class="td-desc">
                  <p class="description prd-desc"><%= product.description || '' %></p>
                  <button type="button" class="read-more" aria-expanded="false">اقرأ المزيد</button>
                </td>
                <td class="td-price">
                  <% const displayPrice = Math.round(Number(product.price));
                     const discount = Number(product.discount || 0);
                  %>
                  <% if (discount > 0) { %>
                    <div class="old-price"><%= displayPrice %> <%= currencySymbol %></div>
                    <div class="new-price"><%= Math.round(displayPrice * (1 - discount / 100)) %> <%= currencySymbol %></div>
                  <% } else { %>
                    <div class="new-price"><%= displayPrice %> <%= currencySymbol %></div>
                  <% } %>
                </td>
                <td class="td-actions">
                  <div class="actions">
                    <button type="button" class="btn btn-sm btn-info" onclick="addToCart('<%= product._id %>', 1)"><i class="fas fa-cart-plus"></i> <span class="btn-text">أضف</span></button>
                    <a class="btn btn-sm btn-primary" href="/products/<%= product._id %>#reviews"><i class="fas fa-star"></i> <span class="btn-text">تقييم</span></a>
                    <a class="btn btn-sm btn-light" href="/products/<%= product._id %>"><i class="fas fa-info-circle"></i> <span class="btn-text">تفاصيل</span></a>
                  </div>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      <% } else { %>
        <div style="text-align: center; padding: 3rem;">
          <i class="fas fa-box-open" style="font-size: 4rem; color: #ddd; margin-bottom: 1rem;"></i>
          <h3 style="color: #666;">لا توجد منتجات حالياً</h3>
        </div>
      <% } %>
    </div>
  </div>

      <% if (totalPages && totalPages > 1) { %>
        <nav class="pagination">
          <% if (currentPage > 1) { %>
            <a class="page-link" href="?page=<%= currentPage - 1 %><%= baseQueryString %>"><i class="fas fa-angle-right"></i></a>
          <% } %>
          
          <% for(let i = 1; i <= totalPages; i++) { %>
            <a class="page-link <%= i === currentPage ? 'active' : '' %>" href="?page=<%= i %><%= baseQueryString %>"><%= i %></a>
          <% } %>
          
          <% if (currentPage < totalPages) { %>
            <a class="page-link" href="?page=<%= currentPage + 1 %><%= baseQueryString %>"><i class="fas fa-angle-left"></i></a>
          <% } %>
        </nav>
      <% } %>
    </div>
  </section>

  <%- include("partials/footer") %>
  <div id="initial-data" style="display:none;" data-selected-id="<%= typeof selectedCategoryId !== 'undefined' ? selectedCategoryId : 'all' %>" data-selected-desc="<%= typeof selectedCategoryDesc !== 'undefined' ? selectedCategoryDesc : '' %>"></div>

  <script>
    // Lightweight image loaded handler: adds .loaded class to product images so CSS fades them in
    document.addEventListener('DOMContentLoaded', () => {
      document.querySelectorAll('.prd-img img').forEach(img => {
        if (img.complete && img.naturalWidth !== 0) {
          img.classList.add('loaded');
        } else {
          img.addEventListener('load', () => img.classList.add('loaded'));
          img.addEventListener('error', () => img.classList.add('loaded'));
        }
      });
    });

    // View toggle (grid/list)
    document.querySelectorAll('.view-toggle button').forEach(btn => {
      btn.addEventListener('click', () => {
        document.querySelectorAll('.view-toggle button').forEach(b => b.classList.remove('active'));
        btn.classList.add('active');
        const grid = document.querySelector('.products-grid');
        if (!grid) return;
        grid.classList.toggle('list-view', btn.dataset.view === 'list');
      });
    });

    // Category filter
    // Animated show/hide helpers for table rows
    function hideRow(row) {
      if (!row) return;
      if (row.style.display === 'none') return;
      row.classList.add('filter-hidden');
      const onEnd = (e) => {
        if (e.target !== row) return;
        row.style.display = 'none';
        row.removeEventListener('transitionend', onEnd);
      };
      row.addEventListener('transitionend', onEnd);
    }

    function showRow(row) {
      if (!row) return;
      row.style.display = '';
      requestAnimationFrame(() => row.classList.remove('filter-hidden'));
    }

    // Category select handler (single-select dropdown)
    const categorySelect = document.getElementById('categorySelect');
    if (categorySelect) {
      categorySelect.addEventListener('change', () => {
        const category = categorySelect.value;
        const selectedOption = categorySelect.options[categorySelect.selectedIndex];
        const desc = selectedOption ? (selectedOption.dataset.desc || '') : '';
        const catDescEl = document.querySelector('.category-description');
        if (catDescEl) catDescEl.textContent = desc;
        const clearBtn = document.getElementById('clearFilterBtn');
        if (clearBtn) clearBtn.style.display = (category && category !== 'all') ? 'inline-block' : 'none';

        document.querySelectorAll('.public-products-table tbody tr, .admin-products-table tbody tr, .product').forEach(row => {
          const rowCat = row.dataset.categoryId || '';
          if (!category || category === 'all') {
            showRow(row);
          } else {
            if (rowCat) {
              if (rowCat === category) showRow(row); else hideRow(row);
            } else {
              const catCell = row.querySelector('.td-cat');
              const txt = catCell ? (catCell.textContent || '').trim().toLowerCase() : '';
              if (txt === category.toString().toLowerCase()) showRow(row); else hideRow(row);
            }
          }
        });
      });
    }

    // (Removed legacy category list/checklist handlers.)

    // Clear filter button
    const clearBtn = document.getElementById('clearFilterBtn');
    if (clearBtn) {
      clearBtn.addEventListener('click', () => {
        // reset select to 'all' and trigger change
        const sel = document.getElementById('categorySelect');
        if (sel) {
          sel.value = 'all';
          sel.dispatchEvent(new Event('change'));
        } else {
          const catDescEl = document.querySelector('.category-description');
          if (catDescEl) catDescEl.textContent = '';
          clearBtn.style.display = 'none';
          document.querySelectorAll('.public-products-table tbody tr, .admin-products-table tbody tr, .product').forEach(row => showRow(row));
        }
      });
    }

    // Search (works for both card-list `.product` and table `.admin-products-table`)
    const searchInput = document.getElementById('searchInput');
    if (searchInput) {
      searchInput.addEventListener('input', (e) => {
        const query = e.target.value.trim().toLowerCase();

        // Rows and cards
        document.querySelectorAll('.public-products-table tbody tr, .admin-products-table tbody tr, .product').forEach(item => {
          // for table rows, look for .td-name; for cards, look for .prd-title
          const nameCell = item.querySelector('.td-name') || item.querySelector('.prd-title') || item.querySelector('td:nth-child(2)');
          const txt = nameCell ? (nameCell.textContent || '').toLowerCase() : '';
          if (txt.includes(query)) showRow(item); else hideRow(item);
        });
      });
    }

    // Sort - update query params and reload while preserving filters/search
    const sortSelect = document.getElementById('sortSelect');
    if (sortSelect) {
      sortSelect.addEventListener('change', (e) => {
        const val = e.target.value;
        const params = new URLSearchParams(window.location.search);
        if (val && val !== 'new') params.set('sort', val); else params.delete('sort');
        // when changing sort, reset to first page
        params.set('page', '1');
        window.location.search = params.toString();
      });
    }

    // Add to cart function
    function addToCart(productId, quantity) {
      fetch('/cart/add', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ productId, quantity })
      })
      .then(res => res.json())
      .then(data => {
        if (data.success) {
          alert('تمت إضافة المنتج إلى السلة');
          // Update cart count in header if exists
          const cartCount = document.getElementById('cart-count');
          if (cartCount && data.cartCount) {
            cartCount.textContent = data.cartCount;
          }
        }
      })
      .catch(err => console.error(err));
    }

    // Wishlist toggle
    function toggleWishlist(productId) {
      // TODO: Implement wishlist functionality
      console.log('Toggle wishlist:', productId);
      alert('سيتم إضافة هذه الميزة قريباً');
    }

    // Quick view
    function quickView(productId) {
      // TODO: Open modal with product details
      window.location.href = `/products/${productId}`;
    }

    // Read-more toggles for product descriptions (3-line clamp -> expand)
    document.addEventListener('DOMContentLoaded', () => {
      document.querySelectorAll('.prd-desc').forEach(p => {
        const btn = p.parentElement ? p.parentElement.querySelector('.read-more') : null;
        if (!btn) return;
        // If content doesn't overflow the clamp, hide the button
        if (p.scrollHeight <= p.clientHeight + 1) {
          btn.style.display = 'none';
          return;
        }
        btn.addEventListener('click', () => {
          const expanded = p.classList.toggle('expanded');
          btn.setAttribute('aria-expanded', expanded ? 'true' : 'false');
          btn.textContent = expanded ? 'عرض أقل' : 'اقرأ المزيد';
        });
      });
    });
  </script>
  <script>
    // Initialize server-side selected category (if any)
    document.addEventListener('DOMContentLoaded', () => {
      try {
        const initEl = document.getElementById('initial-data');
        const selectedId = initEl ? (initEl.dataset.selectedId || 'all') : 'all';
        const desc = initEl ? (initEl.dataset.selectedDesc || '') : '';
        if (selectedId && selectedId !== 'all') {
          const sel = document.getElementById('categorySelect');
          if (sel) {
            sel.value = selectedId;
            sel.dispatchEvent(new Event('change'));
          } else {
            const catDescEl = document.querySelector('.category-description');
            if (catDescEl) catDescEl.textContent = desc || '';
            const clearBtn = document.getElementById('clearFilterBtn');
            if (clearBtn) clearBtn.style.display = 'inline-block';
            document.querySelectorAll('.public-products-table tbody tr, .admin-products-table tbody tr, .product').forEach(row => {
              const rowCat = row.dataset.categoryId || '';
              if (rowCat) {
                if (rowCat === selectedId) showRow(row); else hideRow(row);
              }
            });
          }
        } else {
          const sel = document.getElementById('categorySelect');
          if (sel) sel.value = 'all';
        }
      } catch (e) { console.error('Init category filter error', e); }
    });
  </script>
</body>
</html>